#!ipxe

:ThinStation
echo Booting ThinStation...
set BOOT_IMAGE /boot/vmlinuz
kernel http://${next-server}${BOOT_IMAGE} BOOT_IMAGE=${BOOT_IMAGE} $KERNEL_PARAMETERS boot_device=http
initrd http://${next-server}/boot/initrd
boot

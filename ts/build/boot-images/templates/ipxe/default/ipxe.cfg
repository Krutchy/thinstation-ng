#!ipxe

:ThinStation
echo Booting ThinStation...
set BOOT_IMAGE /boot/vmlinuz
kernel tftp://${next-server}${BOOT_IMAGE} BOOT_IMAGE=${BOOT_IMAGE} $KERNEL_PARAMETERS boot_device=tftp
initrd tftp://${next-server}/boot/initrd
boot

#! /bin/sh

. `dirname $0`/common

if ! pkg_initialized $PACKAGE; then
	pkg_set_init_flag $PACKAGE
	(crontab -l || true; echo "0 * * * * /bin/checklogs")| crontab -
	(crontab -l || true; (set | grep CRON_JOB | cut -d"'" -f2))| crontab -
fi
